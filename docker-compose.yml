services:
  papermind:
    build: .
    ports:
      - "4269:8080"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    env_file: .env
    environment:
      - DB_PATH=/app/data/paper_mind.db
    restart: unless-stopped
